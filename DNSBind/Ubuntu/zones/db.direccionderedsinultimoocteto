;
; BIND reverse data file for local loopback interface
;
$TTL    604800
@       IN      SOA     <fqdn DNS Server>. root.<dominio>. (
                              2         ; Serial
                         604800         ; Refresh
                          86400         ; Retry
                        2419200         ; Expire
                         604800 )       ; Negative Cache TTL
;
        IN      NS      <fqdn DNS>.
1       IN      PTR     <fqdn DNS>ns1.std110lab.dev.      ;       <IP>
1       IN      PTR     <Registros adicionales>.        ; <IP>